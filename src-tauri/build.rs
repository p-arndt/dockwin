fn main() {
    // Standard Tauri v2 build script: generates the context, embeds the frontend
    // assets defined in tauri.conf.json, and wires up Windows resource metadata.
    tauri_build::build();
}
