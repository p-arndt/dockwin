# === Rust ===
/target/
**/target/
**/*.rs.bk
*.pdb
Cargo.lock.orig

# Keep the workspace Cargo.lock checked in for binaries/apps,
# but ignore lock files inside example/scratch crates if any.
# (Workspace root Cargo.lock is intentionally NOT ignored.)

# === Node / frontend ===
node_modules/
**/node_modules/
.pnp
.pnp.js
.npm
.yarn/
.yarn-integrity
.eslintcache
.svelte-kit/
.vite/

# Frontend build output
dist/
dist-ssr/
build/
**/dist/
*.local

# === Tauri ===
# Generated Tauri artifacts and bundles
src-tauri/target/
src-tauri/gen/
# Sidecar CLI binary is built by `pnpm sidecar` from crates/dockwin-cli
src-tauri/binaries/
**/gen/schemas/
**/bundle/
*.app
*.dmg
*.AppImage
*.deb
*.rpm
*.msi
*.msi.zip
*.nsis
*.exe.bundle
WixTools/

# === Logs / env / secrets ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env
.env.*
!.env.example

# === Editors / IDE ===
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.iml
*.swp
*.swo
*~

# === OS cruft ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# === dockwin local scratch ===
/scratch/
*.rootfs.tar
*.rootfs.tar.gz
ubuntu-*-wsl.rootfs.tar*
